# 修改记录

## 版本：BugFix-20260313
**日期：** 2026-03-13
**问题来源：** 资产分析插件Python程序运行异常问题单（编号 20260311-002）
**问题描述：** Python程序启动后运行1-3小时，视频抓拍频率逐渐下降并最终完全停止，重启后可短暂恢复，摄像头扩展至7路后问题加剧

---

## 修改文件清单

### 1. `video_stream_reader.py`（核心修复）

**问题：** `VideoStreamReader` 读取线程连续失败10次后静默退出（`self.running = False`），但 `read()` 方法未检查 `self.running`，导致一直返回最后一帧旧数据。摄像头监测循环误以为流正常，背景建模对同一帧永远检测不到变化，数据产出完全停止且无任何报错。

**修改位置：** 第198行，`read()` 方法

```python
# 修改前
if self.latest_frame is not None:

# 修改后
if self.running and self.latest_frame is not None:
```

**效果：** 读取线程一旦退出，`read()` 立即返回 `(False, None, None)`，触发上层摄像头监测的重连逻辑，自动恢复。

---

### 2. `config.json`

**修改1：** `RTSP_MONITOR_CONFIG.max_reconnect_attempts`

```json
// 修改前
"max_reconnect_attempts": 3

// 修改后
"max_reconnect_attempts": 999
```

**原因：** 原来只重试3次，失败后摄像头监测线程永久退出，依赖 `main.py` 重启。而 `main.py` 最多重启10次，用完后彻底放弃。改为999次后摄像头会持续尝试重连，网络短暂抖动不会导致监测中断。

**修改2：** `YOLO_POOL_CONFIG` 新增 `restart_time`

```json
// 新增
"restart_time": "02:00"
```

**原因：** 配合进程池定时重启功能，指定每天重启时间。可按需修改，例如改为 `"20:00"` 表示晚8点。

---

### 3. `core/yolo_process_pool.py`

**新增方法：** `restart_workers()`

**原因：** YOLO 子进程长时间运行后，Python 内存分配器不会将已释放内存归还给操作系统，导致子进程占用内存持续增长，推理速度随之下降，最终引发超时返回空结果，造成拍摄频率逐渐降低。

**逻辑：**
1. 发送毒丸信号通知所有 Worker 进程优雅退出
2. 等待进程退出（超时则强制终止）
3. 通知所有等待中的检测请求立即失败返回（避免永久阻塞）
4. 清空任务队列和结果队列残余数据
5. 重新启动全部 Worker 进程并等待初始化完成
6. 重置统计信息

**特点：** 摄像头监测线程持有的池引用不变，重启过程对上层透明，重启期间的检测请求快速失败后在下次检测周期自动重试。

---

### 4. `main.py`

**修改1：** 新增 `import` 语句

```python
from datetime import datetime, date
```

**修改2：** `RtspMonitorSystem` 新增 `restart_yolo_pools()` 方法

遍历所有 YOLO 池（默认池和自定义池），逐一调用 `restart_workers()`，异常单独捕获不影响其他池。

**修改3：** 主循环新增定时触发逻辑

每秒检查当前时间是否与 `config.json` 中 `restart_time` 一致，且当天尚未重启过，则自动触发进程池重启。使用 `last_pool_restart_date` 记录日期，防止同一分钟内重复触发。

---

### 5. `core/person_detector_adaptive.py`

**问题：** `preprocess()` 将原始图像尺寸保存为实例变量 `self.orig_width`、`self.orig_height`，在 `postprocess()` 中读取。若未来多线程并发调用同一检测器实例，两个调用的尺寸会互相覆盖，导致检测框坐标计算错误。

**修改：** `preprocess()` 改为同时返回原始尺寸，`postprocess()` 改为接收尺寸参数，`detect_image()` 适配调用方式。

```python
# 修改前
input_tensor = self.preprocess(image)
detections = self.postprocess(outputs)

# 修改后
input_tensor, orig_width, orig_height = self.preprocess(image)
detections = self.postprocess(outputs, orig_width, orig_height)
```

**效果：** 原始尺寸作为局部变量在调用栈内传递，各线程完全隔离，消除竞态条件。

---

### 6. `camera_monitor.py`

**修改位置：** 第95行，`no_person_threshold`

```python
# 修改前
self.no_person_threshold = 30

# 修改后
self.no_person_threshold = 5
```

**原因：** 原来需要连续30次"检测到画面变化但未发现人员"才能从慢速模式（每10秒检测一次）切回快速模式，最长需等待5分钟。当 YOLO 进程因内存压力推理变慢、频繁超时返回空结果时，系统会长时间卡在慢速模式，拍摄频率持续低迷。改为5次后，最多50秒即可恢复快速检测。

---

## 修改汇总

| 文件 | 类型 | 解决的问题 |
|---|---|---|
| `video_stream_reader.py` | Bug修复 | 读取线程死亡后持续返回旧帧，数据产出完全停止 |
| `config.json` | 参数调整 | 重连次数过少导致摄像头永久放弃；新增定时重启时间配置 |
| `core/yolo_process_pool.py` | 新增功能 | YOLO子进程内存积累导致推理变慢 |
| `main.py` | 新增功能 | 定时触发YOLO进程池重启 |
| `core/person_detector_adaptive.py` | 隐患修复 | 多线程下检测框坐标计算可能错乱 |
| `camera_monitor.py` | 参数调整 | 慢速模式恢复过慢，拍摄频率长期低迷 |
