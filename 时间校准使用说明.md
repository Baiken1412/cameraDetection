# PTS时间偏移校准 - 使用说明

## 问题背景

原有实现中，虽然使用了PTS（Presentation Time Stamp）获取帧时间戳，但PTS基准点使用的是程序连接时的系统时间（`datetime.now()`），而不是摄像头的真实拍摄时间。这导致如果RTSP流存在网络传输延迟、编码延迟或缓冲延迟，保存的时间戳会比摄像头实际拍摄时间晚相同的秒数。

**示例**：
- 真实拍摄时间：14:00:00
- RTSP流延迟：5秒（网络2秒 + 编码/缓冲3秒）
- 保存的时间戳：14:00:05 ❌

## 解决方案

实现了**多层次时间校准机制**：

### 1️⃣ 自动校准（推荐）
程序会自动尝试从以下来源获取摄像头的真实时间：
- **RTCP NTP时间戳** - 最准确的方法
- **Stream Metadata** - 从视频流元数据获取时间信息
- **自动估算** - 基于缓冲区延迟估算偏移量

### 2️⃣ 手动校准（最精确）
如果自动校准失败或不准确，可以手动配置时间偏移量。

---

## 配置说明

在 `config.py` 的 `RTSP_MONITOR_CONFIG` 中，已添加以下配置项：

```python
RTSP_MONITOR_CONFIG = {
    # ... 其他配置 ...

    # ==================== 时间校准配置 ====================

    # PTS时间偏移校准（秒）
    # None: 自动检测（推荐）
    # 正值: PTS时间减去此值（如果PTS时间比实际晚，适用于大多数情况）
    # 负值: PTS时间加上此值（如果PTS时间比实际早）
    'pts_time_offset': None,

    # 是否启用时间诊断日志（用于调试时间偏移问题）
    'enable_time_diagnosis': True,

    # 时间校准方法优先级
    # 'auto': 自动选择最佳方法（推荐）
    # 'rtcp': 优先使用RTCP NTP时间戳
    # 'stream_metadata': 使用stream metadata中的时间信息
    # 'manual': 只使用手动配置的pts_time_offset
    'time_calibration_method': 'auto',
}
```

---

## 使用步骤

### 步骤1：启用时间诊断（首次使用）

确保配置中 `enable_time_diagnosis` 为 `True`：

```python
'enable_time_diagnosis': True,
```

### 步骤2：启动程序并观察日志

运行程序后，观察日志输出：

#### ✅ 成功案例：自动校准成功
```
✓ 从RTCP获取到NTP时间 - 摄像头: 门口摄像头, NTP时间: 14:00:00.123, 时间偏移: 5.23秒
✓ 时间偏移校准完成 - 摄像头: 门口摄像头, 偏移量: 5.23秒, 校准方法: rtcp
⏱️ 时间诊断 - 摄像头: 门口摄像头, PTS时间: 14:00:00.156, 系统时间: 14:00:00.234, 时间差: 0.08秒
```
**说明**：时间差 < 1秒，校准成功！✅

#### ⚠️ 警告案例：自动校准失败，需手动配置
```
⚠️ 无法自动校准时间偏移 - 摄像头: 门口摄像头, 将使用默认值0秒（可能存在延迟）
💡 建议：在config.py中设置 'pts_time_offset' 参数来手动校准时间偏移
⏱️ 时间诊断 - 摄像头: 门口摄像头, PTS时间: 14:00:05.156, 系统时间: 14:00:00.234, 时间差: -4.92秒
⚠️ 时间偏移较大 (-4.92秒) - 摄像头: 门口摄像头, 建议在config.py中手动设置 'pts_time_offset': -4.9
```
**说明**：时间差较大，需要手动校准！

### 步骤3：手动校准（如需要）

根据日志提示，在 `config.py` 中设置 `pts_time_offset`：

```python
# 示例：如果日志显示 "时间差: -4.92秒"，建议值为 -4.9
'pts_time_offset': -4.9,  # 手动设置偏移量
```

**重要提示**：
- **正值**：PTS时间比系统时间晚，减去此值（最常见情况）
- **负值**：PTS时间比系统时间早，加上此值

### 步骤4：重启程序验证

重启程序后，观察时间诊断日志：

```
使用手动配置的时间偏移 - 摄像头: 门口摄像头, 偏移: -4.90秒
⏱️ 时间诊断 - 摄像头: 门口摄像头, PTS时间: 14:00:00.234, 系统时间: 14:00:00.256, 时间差: 0.02秒
```

**时间差 < 1秒即为成功！** ✅

---

## 验证方法

### 方法1：对比手表时间
1. 在摄像头视野中，用手机显示当前时间（精确到秒）
2. 触发检测，查看保存的图片时间戳
3. 对比图片时间戳与手机显示时间，误差应 < 1秒

### 方法2：查看数据库记录
```sql
SELECT
    sxtmx AS 摄像头名称,
    pssj AS 拍摄时间,
    NOW() AS 当前系统时间,
    TIMESTAMPDIFF(SECOND, pssj, NOW()) AS 时间差秒数
FROM app_track
ORDER BY pssj DESC
LIMIT 10;
```

**正常情况**：时间差秒数应接近实际经过时间（±1秒以内）

### 方法3：观察日志
启用 `enable_time_diagnosis` 后，每次保存检测结果都会输出时间诊断信息：
- **PTS时间**：根据视频流PTS计算的时间（已校准）
- **系统时间**：程序运行的系统时间
- **时间差**：两者差异（理想情况下应 < 1秒）

---

## 常见问题

### Q1: 时间诊断显示时间差很大（>5秒），怎么办？
**A**: 需要手动校准：
1. 观察日志中的建议值：`建议在config.py中手动设置 'pts_time_offset': X.X`
2. 在config.py中设置该值
3. 重启程序验证

### Q2: 不同摄像头延迟不同，能分别配置吗？
**A**: 当前版本使用全局配置。如果不同摄像头延迟差异很大，建议：
- 选择延迟最大的摄像头作为基准
- 或者修改代码支持按摄像头ID配置（需二次开发）

### Q3: 校准后时间还是不准，可能的原因？
**A**: 检查以下几点：
1. **系统时间是否准确** - 运行 `date` 命令检查服务器时间
2. **摄像头时间是否同步** - 登录摄像头Web界面，检查设备时间
3. **网络延迟是否稳定** - 使用 `ping` 测试摄像头IP延迟波动
4. **RTSP流是否稳定** - 检查是否频繁重连

### Q4: 如何禁用时间诊断日志？
**A**: 在config.py中设置：
```python
'enable_time_diagnosis': False,
```

---

## 技术原理

### 原有问题
```python
# 原实现（有问题）
self.pts_base_time = datetime.now()  # ❌ 使用系统时间作为基准
```

### 修复后
```python
# 新实现（已修复）
# 1. 建立PTS基准
self.pts_base_time = datetime.now()

# 2. 自动计算时间偏移（从RTCP/metadata获取摄像头真实时间）
self.time_offset_seconds = self._auto_calibrate_time_offset()

# 3. 应用时间偏移
real_time = self.pts_base_time + timedelta(seconds=(pts - pts_base_offset))
real_time -= timedelta(seconds=self.time_offset_seconds)  # ✅ 修正延迟
```

### 时间流转示意图
```
摄像头拍摄 → 编码 → 网络传输 → 程序接收 → PTS转换 → 应用偏移校准 → 保存时间戳
14:00:00     (+1s)    (+2s)      14:00:03    14:00:03   (-3s)        14:00:00 ✅
```

---

## 更新日志

**2024-12-XX**
- ✅ 添加RTCP NTP时间戳自动获取
- ✅ 添加Stream Metadata时间信息解析
- ✅ 实现自动时间偏移计算
- ✅ 支持手动时间偏移配置
- ✅ 添加时间诊断日志功能
- ✅ 在config中添加时间校准相关配置

---

## 联系支持

如果遇到问题，请提供以下信息：
1. 时间诊断日志（包含"⏱️ 时间诊断"的日志行）
2. 摄像头型号和RTSP地址格式
3. 网络环境（局域网/广域网）
4. config.py中的时间校准配置
