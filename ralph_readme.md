# cameraDetection — Ralph 方法集成说明

## 一、项目概况

`cameraDetection` 是一套基于深度学习的 RTSP 视频流实时人员检测监测系统，基于 YOLOv8/v11 + OpenVINO/ONNX，支持多摄像头并发、ReID 跨镜头追踪、YOLO 实例池、背景差分变化检测等功能。技术栈为 Python + Flask + MySQL + OpenCV。

---

## 二、与 Ralph 模板的对比分析

| Ralph 模板要素 | 模板要求 | cameraDetection 现状 | 差距评级 |
|---|---|---|---|
| **specs/** 需求目录 | 每个关注主题一个 `.md` 文件 | 缺失，需求散落在多个中文文档中 | **缺失** |
| **AGENTS.md** 操作手册 | ~60行，构建/运行/验证命令 | 缺失 | **缺失** |
| **IMPLEMENTATION_PLAN.md** | 可处置的优先级任务列表 | 缺失 | **缺失** |
| **PROMPT_plan.md** | 规划模式指令 | 缺失 | **缺失** |
| **PROMPT_build.md** | 构建模式指令 | 缺失 | **缺失** |
| **PROMPT_fix.md** | Bug修复模式指令 | 缺失 | **缺失** |
| **loop.ps1** 编排脚本 | 自动循环执行 | 缺失 | **缺失** |
| **specs/bugs/** Bug目录 | 结构化 Bug 描述 | 缺失 | **缺失** |
| **tests/** 测试套件 | 回压机制核心 | **已有** 5个测试文件 + pytest | 较好 |
| **src/** 源码目录 | 规范化源码结构 | 源码在根目录，未归入 `src/` | 部分符合 |
| **.gitignore** | 版本控制规则 | **已有** | 符合 |

---

## 三、现有项目的优势（Ralph 视角）

1. **回压机制基础已有**：`tests/` 目录有 5 个测试文件，涵盖 InputShaper、YOLO 池、系统集成、慢流隔离、时间戳一致性
2. **配置系统成熟**：`config.json` + `config.py` 分层加载，符合 Ralph 推崇的"配置即代码"
3. **文档积累丰富**：`功能说明.md`、`测试功能.md`、`系统业务功能分析.md` 等，可作为 specs 的素材
4. **模块化设计良好**：`core/` 目录封装核心算法、`utils/` 封装工具类、`pipeline/` 封装流水线，职责分离清晰
5. **设计模式运用合理**：单例（DB连接池）、对象池（YOLO池）、生产者-消费者（视频流）、策略（CPU检测选引擎）

---

## 四、已创建的 Ralph 方法控制文件

### 流程控制文件（6 个）

| 文件 | 大小 | 说明 |
|---|---|---|
| `AGENTS.md` | 7.3 KB | 操作手册：项目结构、执行规则、验证命令、提交规则 |
| `PROMPT_plan.md` | 3.0 KB | 规划模式指令：差距分析 → 生成执行计划 |
| `PROMPT_build.md` | 3.3 KB | 构建模式指令：选任务 → 实现 → 测试 → 提交 |
| `PROMPT_fix.md` | 4.1 KB | 修复模式指令：最小上下文 → 定位 → 修复 → 提交 |
| `IMPLEMENTATION_PLAN.md` | 0.5 KB | 执行计划占位（待规划模式生成） |
| `loop.ps1` | 2.1 KB | Ralph 循环执行脚本（三种模式切换） |

### 需求规格文件（9 个）

| 文件 | 关注主题 |
|---|---|
| `specs/视频流接入与帧率稳定.md` | RTSP 流读取、InputShaper、缓冲区管理 |
| `specs/人员检测与YOLO推理.md` | YOLOv11、OpenVINO/ONNX 自适应选择 |
| `specs/多摄像头并发与YOLO池.md` | 共享池、独立池、多进程池 |
| `specs/背景差分变化检测.md` | MOG2/KNN 算法、三重过滤 |
| `specs/跨镜头ReID追踪.md` | 特征提取、聚类、跨镜头匹配 |
| `specs/数据库与存储.md` | MySQL 连接池、图像存储、轨迹管理 |
| `specs/Web界面与API.md` | Flask REST API、Dashboard |
| `specs/授权许可系统.md` | RSA 许可证生成与验证 |
| `specs/系统启动与监测编排.md` | 初始化流程、多摄像头编排、日志 |
| `specs/bugs/BUG_TEMPLATE.md` | Bug 描述模板 |

---

## 五、总结

| 维度 | 评分 | 说明 |
|---|---|---|
| **代码质量** | 高 | 模块化好，设计模式合理，配置分层 |
| **测试覆盖** | 中 | 有核心模块测试，但缺少 API/数据库/ReID 测试 |
| **文档积累** | 中 | 有功能文档，但缺少结构化需求规格 |
| **Ralph 适配度** | 低 → 已补齐 | 全部流程控制文件已创建 |
| **适配难度** | 低 | 基础扎实，补充 Ralph 控制文件即可接入 |

**核心结论**：`cameraDetection` 是一个工程质量不错的项目，模块设计和测试基础都已具备。要接入 Ralph 方法，主要工作是**补充流程控制层**（specs、AGENTS.md、PROMPT 文件、loop.ps1），不需要大幅改动现有代码。现有的中文文档已直接拆分转化为 Ralph 所需的 specs 文件。

---

## 六、使用方式

在 `D:\sacw\cameraDetection` 目录下运行：

```powershell
# 1. 首次使用：生成执行计划
.\loop.ps1 -Mode plan

# 2. 开发迭代：逐个任务构建
.\loop.ps1 -Mode build

# 3. 快速修复 Bug
#    先在 specs/bugs/ 下创建 BUG_001.md，然后：
.\loop.ps1 -Mode fix
```

### 典型工作流

**新增需求**：
1. 在 `specs/` 下新增或修改对应主题的 `.md` 文件
2. 运行 `.\loop.ps1 -Mode plan`（重新生成执行计划）
3. 运行 `.\loop.ps1 -Mode build`（逐个任务构建）

**修复 Bug**：
1. 复制 `specs/bugs/BUG_TEMPLATE.md` 为 `specs/bugs/BUG_001.md`
2. 填写 Bug 现象、预期、复现步骤
3. 运行 `.\loop.ps1 -Mode fix`

**重新规划**（计划偏离时）：
1. 删除或清空 `IMPLEMENTATION_PLAN.md`
2. 运行 `.\loop.ps1 -Mode plan`


创建 Ralph 方法控制文件,对我以后的修改有什么好处？

● 这个问题的核心在于：Ralph 方法解决的是"人 + AI 协作开发"中的哪些痛点。

  ---
  没有 Ralph 时，你日常修改代码的典型问题

  1. 每次对话都要重新交代背景

  你跟 Claude 说"帮我修一个 YOLO 池的 bug"，Claude 不知道你的项目结构、不知道代码在哪、不知道怎么跑测试。你得反复解释。

  有了 Ralph：每次循环自动加载 AGENTS.md，Claude 直接知道项目结构、运行命令、测试命令、代码约定。零解释成本。

  ---
  2. AI 改着改着就跑偏

  你让它修一个 bug，它顺手重构了三个文件、改了配置格式、还加了一堆注释。改完你发现测试跑不过了，而且不知道它改了什么。

  有了 Ralph：
  - PROMPT_fix.md 明确限制"只修改与 Bug 直接相关的代码"、"不得顺便优化"
  - PROMPT_build.md 限制"每次只做一个任务"
  - 回压规则要求"测试不过不许提交"

  ---
  3. 大功能不知道从哪下手

  比如你想给系统加一个"按区域统计人流量"的功能，涉及数据库、API、前端。直接让 AI 写，它可能一口气改十个文件，改到一半 token 耗尽，留下一堆半成品。

  有了 Ralph：
  1. 先把需求写进 specs/人流量统计.md
  2. 跑 loop.ps1 -Mode plan，AI 自动做差距分析，拆出 5 个可执行任务
  3. 跑 loop.ps1 -Mode build，每轮只做一个任务，做完提交，下一轮从磁盘读取最新状态继续

  每个任务都是完整的、可测试的、已提交的。不会出现半成品。

  ---
  4. 上下文窗口被浪费

  一次对话中，你让 AI 读了大量文件、讨论了半天方案，真正写代码时 token 已经不够了，AI 开始"变笨"。

  有了 Ralph：
  - 每轮循环冷启动，只加载必要的文件（PROMPT + AGENTS.md + 计划）
  - 修 bug 时用 fix 模式，只加载 bug 描述 + 相关代码，节省约 70% token
  - AI 始终在"聪明区间"工作

  ---
  5. 不知道改到哪了、还剩什么没做

  改了三天，忘了哪些做完了、哪些还没开始。

  有了 Ralph：IMPLEMENTATION_PLAN.md 是唯一的状态来源，每次任务完成后自动更新。打开就知道当前进度。

  ---
  一句话总结
  ┌────────────┬─────────────────────────────────┬─────────────────────────────────────────────────┐
  │    场景    │           没有 Ralph            │                    有 Ralph                     │
  ├────────────┼─────────────────────────────────┼─────────────────────────────────────────────────┤
  │ 修一个 bug │ 先花 10 分钟给 AI 解释项目      │ specs/bugs/BUG_001.md → loop.ps1 -Mode fix      │
  ├────────────┼─────────────────────────────────┼─────────────────────────────────────────────────┤
  │ 加一个功能 │ AI 一口气改十个文件，改到一半崩 │ plan 拆任务 → build 逐个做 → 每个都可测试可提交 │
  ├────────────┼─────────────────────────────────┼─────────────────────────────────────────────────┤
  │ 中途被打断 │ 下次重新来，之前改的可能白费    │ 状态在磁盘上，下次冷启动自动接续                │
  ├────────────┼─────────────────────────────────┼─────────────────────────────────────────────────┤
  │ 多人协作   │ 每个人跟 AI 说的不一样          │ specs 是统一的需求来源，AGENTS.md 是统一的规则  │
  └────────────┴─────────────────────────────────┴─────────────────────────────────────────────────┘
  本质上，Ralph 把"跟 AI 聊天写代码"变成了"用工程方法驱动 AI 写代码"。 对你的 cameraDetection 这种有十几个模块、多种运行模式的生产级项目来说，这个差距会随着项目复杂度增长越来越明显。
