# 系统诊断工具使用说明

## 概述

`diagnose.py` 是一个用于排查摄像头监控系统问题的诊断工具，可以检测：
- 运行环境和依赖
- 硬件性能（CPU、内存、磁盘）
- 网络连通性
- 视频流质量（清晰度、帧率、丢帧率）
- 保存图片质量
- 日志错误统计

## 使用方式

### 命令行运行

```bash
# 完整诊断（默认从数据库读取所有摄像头）
python diagnose.py

# 测试指定的 RTSP 地址
python diagnose.py --rtsp "rtsp://admin:CHANGE_ME@192.168.100.100:554/stream"

# 快速检查（跳过视频源检测）
python diagnose.py --quick

# 指定 ping 测试目标
python diagnose.py --ping 192.168.1.1 192.168.100.100
```

### 定时自动诊断

在 `config.json` 中配置：

```json
"DIAGNOSE_CONFIG": {
    "enabled": true,
    "interval_minutes": 30,
    "output_dir": "diagnosis_reports"
}
```

| 参数 | 说明 | 默认值 |
|-----|------|-------|
| `enabled` | 是否启用定时诊断 | `false` |
| `interval_minutes` | 诊断间隔（分钟） | `30` |
| `output_dir` | 报告输出目录 | `diagnosis_reports` |

启用后，系统启动时会立即运行一次诊断，之后每隔指定时间自动运行。

## 诊断项说明

### 1. 运行环境检查

检测项：
- Python 版本
- OpenCV、NumPy、PyTorch 版本
- CUDA 是否可用
- ONNX Runtime / OpenVINO 推理引擎

### 2. 硬件性能检查

| 检测项 | 告警阈值 | 说明 |
|-------|---------|------|
| CPU 占用率 | > 80% | 可能导致处理延迟 |
| 内存占用率 | > 85% | 可能导致程序崩溃 |
| 磁盘空间 | > 90% 或 < 10GB | 无法保存图片 |
| 磁盘写入速度 | < 10 MB/s | 可能导致丢帧 |

### 3. 网络检查

- 默认 ping `114.114.114.114`
- 自动从数据库提取摄像头 IP 进行测试
- 延迟 > 100ms 会告警

### 4. 视频源检查

从数据库 `app_roomip` 表读取摄像头 RTSP 地址，检测：

| 检测项 | 告警阈值 | 可能原因 |
|-------|---------|---------|
| 连接耗时 | > 5秒 | 网络延迟高 |
| 分辨率 | < 640x480 | 摄像头配置低 |
| 实际帧率 | < 5 fps | 网络带宽不足 |
| 丢帧率 | > 10% | 网络不稳定 |
| 清晰度 | < 100 | 对焦问题/网络丢包/镜头脏 |
| 亮度 | < 10 或 > 245 | 曝光异常 |

### 5. 日志错误检查

统计最近 1000 行日志中的错误：
- 连接失败
- 读取帧失败
- 超时
- 重连
- 断流

### 6. 图片质量检查

检查 `image_save_path` 目录下最近 10 张图片：

| 检测项 | 告警阈值 | 说明 |
|-------|---------|------|
| 分辨率 | < 640x480 | 图片过小 |
| 文件大小 | < 20KB | 压缩过度 |
| 清晰度 | < 100 | 图片模糊 |

## 诊断报告

每次诊断生成 JSON 格式报告，保存为 `diagnosis_YYYYMMDD_HHMMSS.json`。

报告结构：
```json
{
  "timestamp": "2024-01-19T14:30:00",
  "environment": { ... },
  "hardware": { ... },
  "network": { ... },
  "video_sources": [ ... ],
  "issues": ["发现的问题列表"],
  "recommendations": ["建议列表"]
}
```

## 常见问题排查

### 图片/视频不清晰

| 诊断结果 | 可能原因 | 解决方案 |
|---------|---------|---------|
| 清晰度 < 100 | 摄像头对焦问题 | 调整摄像头焦距 |
| 清晰度 < 100 | 镜头脏污 | 清洁镜头 |
| 丢帧率 > 10% | 网络传输丢包 | 检查网线/交换机 |
| 实际帧率低 | 带宽不足降码率 | 升级网络带宽 |
| 文件过小 | 压缩参数过低 | 调整 imwrite 质量参数 |

### 监测不稳定

| 诊断结果 | 可能原因 | 解决方案 |
|---------|---------|---------|
| Ping 超时 | 网络不通 | 检查网络连接 |
| 连接耗时 > 5秒 | 网络延迟高 | 优化网络拓扑 |
| CPU > 80% | 处理能力不足 | 减少摄像头数量或升级硬件 |
| 内存 > 85% | 内存不足 | 增加内存 |
| 磁盘写入 < 10MB/s | I/O 瓶颈 | 更换 SSD |
| 日志频繁重连 | 摄像头或网络不稳定 | 检查摄像头供电和网络 |

## 数据来源

| 数据 | 来源 |
|-----|------|
| 视频流 | 数据库 `app_roomip.rtspssl` 字段 |
| 保存图片 | `config.json` 中 `RTSP_MONITOR_CONFIG.image_save_path` |
| 数据库连接 | `config.json` 中 `DATABASE_CONFIG` |

## 依赖

必需：
- opencv-python
- loguru
- pymysql

可选（增强功能）：
- psutil（硬件性能检测）
- torch（GPU 检测）
- onnxruntime / openvino（推理引擎检测）

安装可选依赖：
```bash
pip install psutil
```
