# 软件许可证系统 - 完整使用教程

## 📋 目录
1. [首次设置（生成密钥对）](#1-首次设置生成密钥对)
2. [客户申请许可证流程](#2-客户申请许可证流程)
3. [生成并发送许可证](#3-生成并发送许可证)
4. [客户安装许可证](#4-客户安装许可证)
5. [常见问题](#5-常见问题)

---

## 1. 首次设置（生成密钥对）

**只需要做一次！**

### 步骤 1.1：生成密钥对

```bash
python generate_license.py --generate-keys
```

输出：
```
============================================================
生成RSA密钥对...
============================================================
私钥已保存到: private_key.pem
公钥已保存到: public_key.pem

密钥对生成成功！
- private_key.pem: 私钥文件（请妥善保管，用于生成许可证）
- public_key.pem: 公钥文件（需要随软件一起发布，用于验证许可证）
============================================================
```

### 步骤 1.2：备份私钥

**⚠️ 重要：**
- `private_key.pem` 是核心机密，务必备份到安全位置
- 如果丢失，将无法为客户生成许可证
- 建议备份到加密U盘、云盘加密文件夹等

---

## 2. 客户申请许可证流程

### 方法一：客户运行主程序获取机器码（推荐）

客户在其电脑上运行：

```bash
python main.py
```

**如果没有许可证，会显示：**
```
============================================================
正在验证软件许可证...
============================================================
机器码: 85003c68c4b40770027d59063acfe12789b775154c36c59df2232a264387ff3c

============================================================
【许可证验证失败】
============================================================
当前机器码: 85003c68c4b40770027d59063acfe12789b775154c36c59df2232a264387ff3c

请联系软件提供商获取有效的许可证文件。
需要提供上述机器码以生成对应的许可证。
============================================================

按Enter键退出...
```

客户将**机器码**发给你。

### 方法二：使用工具获取机器码

客户也可以运行：

```bash
python generate_license.py --show-machine-code
```

输出：
```
============================================================
获取当前机器码...
============================================================

机器码: 85003c68c4b40770027d59063acfe12789b775154c36c59df2232a264387ff3c

请将此机器码提供给客户，用于生成许可证。
============================================================
```

**注意：方法二需要把 `generate_license.py` 给客户，不推荐。建议使用方法一。**

---

## 3. 生成并发送许可证

### 步骤 3.1：生成许可证

收到客户的机器码后，运行：

**1年有效期（365天）：**
```bash
python generate_license.py --create --machine-code 85003c68c4b40770027d59063acfe12789b775154c36c59df2232a264387ff3c --days 365
```

**3年有效期（1095天）：**
```bash
python generate_license.py --create --machine-code 85003c68c4b40770027d59063acfe12789b775154c36c59df2232a264387ff3c --days 1095
```

**永久许可（9999天）：**
```bash
python generate_license.py --create --machine-code 85003c68c4b40770027d59063acfe12789b775154c36c59df2232a264387ff3c --days 9999
```

输出：
```
============================================================
生成许可证...
============================================================
许可证已保存到: license_85003c68.dat

许可证生成成功！
- 文件名: license_85003c68.dat
- 机器码: 85003c68c4b40770027d59063acfe12789b775154c36c59df2232a264387ff3c
- 有效期: 365 天
- 签发时间: 2026-01-05 13:51:49
- 过期时间: 2027-01-05 13:51:49

请将以下文件一起发送给客户:
1. license_85003c68.dat (许可证文件，需改名为 license.dat)
2. public_key.pem (公钥文件)
============================================================
```

### 步骤 3.2：准备发送给客户的文件

创建一个文件夹，放入以下文件：

```
客户软件包/
├── license_85003c68.dat    ← 许可证文件（告诉客户改名）
├── public_key.pem          ← 公钥文件
└── 安装说明.txt             ← 可选：写一个简单说明
```

**安装说明.txt 内容示例：**
```
软件许可证安装说明

1. 将 license_85003c68.dat 改名为 license.dat
2. 将 license.dat 和 public_key.pem 复制到软件根目录（main.py 所在目录）
3. 运行 python main.py 启动软件

如有问题，请联系技术支持。
```

### 步骤 3.3：发送给客户

通过邮件、微信、网盘等方式发送给客户。

---

## 4. 客户安装许可证

### 步骤 4.1：改名许可证文件

客户将 `license_85003c68.dat` 改名为 `license.dat`

**Windows：**
- 右键文件 → 重命名 → 改为 `license.dat`

**Linux/Mac：**
```bash
mv license_85003c68.dat license.dat
```

### 步骤 4.2：放到软件目录

将文件放到软件根目录（与 `main.py` 同级）：

```
软件目录/
├── main.py
├── license.dat        ← 放这里
├── public_key.pem     ← 放这里
├── license_manager.py
├── config.py
└── ... 其他文件
```

### 步骤 4.3：运行软件

```bash
python main.py
```

**成功的输出：**
```
============================================================
正在验证软件许可证...
============================================================

============================================================
【许可证验证成功】
============================================================

========== RTSP监测系统初始化完成（使用背景建模检测） ==========
... (软件正常启动)
```

---

## 5. 常见问题

### Q1：提示"许可证文件不存在"

**原因：**
- 没有 `license.dat` 文件
- 文件名没改对（还是 `license_85003c68.dat`）
- 文件位置不对

**解决：**
1. 确认文件名是 `license.dat`（不是 `.txt` 等其他后缀）
2. 确认文件在软件根目录（与 `main.py` 同级）

### Q2：提示"公钥文件不存在"

**原因：**
- 没有 `public_key.pem` 文件

**解决：**
- 从软件提供商那里获取 `public_key.pem` 文件
- 放到软件根目录

### Q3：提示"许可证与当前机器不匹配"

**原因：**
- 许可证是为其他机器生成的
- 更换了主要硬件（CPU、主板、硬盘等）

**解决：**
1. 重新获取当前机器的机器码（运行 `python main.py`）
2. 联系软件提供商重新生成许可证

### Q4：提示"许可证已过期"

**原因：**
- 许可证超过有效期

**解决：**
- 联系软件提供商申请续期许可证

### Q5：提示"许可证签名验证失败"

**原因：**
- 许可证文件损坏或被篡改
- 公钥文件不匹配

**解决：**
1. 重新下载许可证文件和公钥文件
2. 如果还不行，联系软件提供商

### Q6：更换硬件后许可证失效

**原因：**
- 机器码基于硬件生成，更换主要硬件会改变机器码

**解决：**
1. 重新获取机器码
2. 联系软件提供商重新生成许可证

**建议：**
- 更换硬件前先联系软件提供商
- 软件提供商可以考虑提供一定次数的免费重新授权

---

## 6. 命令速查表

### 软件提供商使用

```bash
# 首次生成密钥对（只做一次）
python generate_license.py --generate-keys

# 为客户生成1年期许可证
python generate_license.py --create --machine-code <客户机器码> --days 365

# 为客户生成永久许可证
python generate_license.py --create --machine-code <客户机器码> --days 9999

# 验证许可证文件
python generate_license.py --verify license_85003c68.dat

# 查看帮助
python generate_license.py --help
```

### 客户使用

```bash
# 运行软件（会自动验证许可证）
python main.py

# 如果需要查看机器码（可选）
python generate_license.py --show-machine-code
```

---

## 7. 文件保密清单

### ✅ 可以给客户的文件
- `license_manager.py`（完整代码也没关系）
- `public_key.pem`（公钥）
- `license.dat`（许可证）
- `main.py` 及其他业务代码

### ❌ 绝对不能给客户的文件
- `private_key.pem`（私钥 - 核心机密！）
- `generate_license.py`（不是必须保密，但建议不给，避免混淆）

---

## 8. 安全建议

1. **私钥管理**
   - 定期备份 `private_key.pem` 到多个安全位置
   - 不要通过不安全的渠道传输私钥
   - 如果私钥泄露，立即重新生成密钥对

2. **许可证发放**
   - 记录每个客户的机器码和许可证有效期
   - 可以使用Excel表格管理客户信息

3. **续期管理**
   - 可以提前提醒客户许可证即将到期
   - 续期时使用相同的机器码重新生成即可

---

## 9. 完整示例演示

### 场景：为新客户"张三公司"生成许可证

**第1步：客户发来机器码**
```
张三: 我这边运行软件显示需要许可证，机器码是：
85003c68c4b40770027d59063acfe12789b775154c36c59df2232a264387ff3c
```

**第2步：生成许可证（1年期）**
```bash
python generate_license.py --create --machine-code 85003c68c4b40770027d59063acfe12789b775154c36c59df2232a264387ff3c --days 365
```

**第3步：准备文件**
```
客户软件包-张三公司/
├── license_85003c68.dat
├── public_key.pem
└── 安装说明.txt
```

**第4步：发给客户，并说明**
```
张三您好，

许可证文件已生成，请：
1. 将 license_85003c68.dat 改名为 license.dat
2. 将 license.dat 和 public_key.pem 放到软件目录
3. 运行 python main.py

有效期至：2027-01-05

如有问题请联系我。
```

**第5步：客户操作**
- 改名：`license_85003c68.dat` → `license.dat`
- 复制到软件目录
- 运行：`python main.py`
- 成功启动！

---

**搞定！有问题随时联系。**
