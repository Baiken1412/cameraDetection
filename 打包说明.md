# 软件打包说明

## ✅ 许可证系统完全支持打包

当前的许可证系统可以直接打包成 exe，无需修改代码！

---

## 📦 打包步骤

### 1. 安装 PyInstaller

```bash
pip install pyinstaller
```

### 2. 打包主程序

#### 方式A：打包成单个exe（简单）

```bash
pyinstaller --onefile main.py
```

**优点：** 只有一个exe文件，方便分发
**缺点：** 启动较慢，杀毒软件可能误报

#### 方式B：打包成文件夹（推荐）

```bash
pyinstaller --onedir main.py
```

**优点：** 启动快，兼容性好
**缺点：** 文件较多

#### 高级选项（可选）

```bash
# 打包时添加图标、隐藏控制台等
pyinstaller --onefile --icon=icon.ico --noconsole main.py

# 如果有额外的数据文件需要打包
pyinstaller --onefile --add-data "config.ini;." main.py
```

**注意：** `license.dat` 和 `public_key.pem` 不要用 `--add-data` 打包进去，必须放在外部！

---

## 📂 打包后的文件结构

### 单文件模式（--onefile）

打包完成后，在 `dist` 目录：

```
dist/
└── main.exe                ← 生成的exe
```

**发给客户时的结构：**

```
客户软件包/
├── main.exe                ← 可执行文件
├── license.dat             ← 许可证（必须！）
├── public_key.pem          ← 公钥（必须！）
└── 使用说明.txt             ← 可选
```

### 文件夹模式（--onedir）

打包完成后，在 `dist` 目录：

```
dist/
└── main/
    ├── main.exe            ← 主程序
    ├── _internal/          ← 依赖库文件夹
    │   ├── python311.dll
    │   ├── cryptography/
    │   └── ... 其他依赖
    └── ... 其他文件
```

**发给客户时的结构：**

```
客户软件包/
└── main/
    ├── main.exe            ← 可执行文件
    ├── _internal/          ← 依赖库
    ├── license.dat         ← 许可证（必须！）
    ├── public_key.pem      ← 公钥（必须！）
    └── 使用说明.txt         ← 可选
```

---

## 🎯 关键注意事项

### ⚠️ 必须遵守的规则

1. **许可证文件必须在外部**
   - `license.dat` 和 `public_key.pem` 必须和 exe 在同一目录
   - **不能**打包进 exe 内部
   - 客户可以随时更换许可证文件

2. **文件路径问题**
   - 当前代码使用相对路径，打包后会从 exe 所在目录查找
   - 客户必须在 exe 所在目录运行程序（不要双击快捷方式）
   - 或者创建快捷方式时设置"起始位置"

3. **依赖库检查**
   - PyInstaller 会自动打包 `cryptography` 库
   - 如果打包失败，检查是否缺少依赖

---

## 🧪 测试打包结果

### 1. 本地测试

```bash
# 1. 打包
pyinstaller --onefile main.py

# 2. 进入 dist 目录
cd dist

# 3. 复制许可证文件（测试用）
copy ..\license.dat .
copy ..\public_key.pem .

# 4. 运行测试
main.exe
```

### 2. 清洁环境测试

在没有安装 Python 的电脑上测试：
1. 复制 `dist` 目录到测试电脑
2. 添加 `license.dat` 和 `public_key.pem`
3. 双击 `main.exe` 运行
4. 检查许可证验证是否正常

---

## 🔧 常见问题

### Q1: 打包后提示"找不到 license.dat"

**原因：** 许可证文件不在 exe 同级目录

**解决：**
```
✓ 正确：
  main.exe
  license.dat          ← 同级
  public_key.pem       ← 同级

✗ 错误：
  main.exe
  licenses/
    license.dat        ← 不同级
```

### Q2: 打包后提示"找不到 public_key.pem"

**解决：** 同上，确保 `public_key.pem` 和 exe 在同一目录

### Q3: 打包后提示"无法获取机器码"

**原因：** Windows 命令可能在打包后无法执行

**解决：** 当前代码已经兼容，如果还有问题，检查杀毒软件是否拦截

### Q4: 打包后杀毒软件报毒

**原因：** PyInstaller 打包的 exe 可能被误报

**解决：**
1. 使用 `--onedir` 模式打包（误报率更低）
2. 代码签名（需要购买证书）
3. 向杀毒软件厂商申报白名单

### Q5: 打包后无法生成许可证

**解决：** `generate_license.py` 也需要单独打包：

```bash
pyinstaller --onefile generate_license.py
```

但**不要**把 `generate_license.exe` 发给客户！

---

## 📋 完整打包流程示例

### 场景：打包软件发给客户

#### 第1步：打包主程序

```bash
# 在项目目录运行
pyinstaller --onefile --name RTSP监测系统 main.py
```

#### 第2步：准备客户软件包

```bash
# 创建客户包目录
mkdir 客户软件包

# 复制exe
copy dist\RTSP监测系统.exe 客户软件包\

# 复制公钥（所有客户通用）
copy public_key.pem 客户软件包\

# 为客户生成许可证
python generate_license.py --create --machine-code <客户机器码> --days 365

# 复制并改名许可证
copy license_85003c68.dat 客户软件包\license.dat
```

#### 第3步：客户收到的文件

```
客户软件包/
├── RTSP监测系统.exe
├── license.dat
├── public_key.pem
└── 使用说明.txt
```

#### 第4步：客户使用

双击 `RTSP监测系统.exe` 即可运行！

---

## 🚀 自动化打包脚本（可选）

创建 `build.bat`（Windows）：

```batch
@echo off
echo ========================================
echo 开始打包 RTSP监测系统
echo ========================================

REM 清理旧的打包文件
if exist dist rmdir /s /q dist
if exist build rmdir /s /q build

REM 打包
pyinstaller --onefile --name RTSP监测系统 main.py

REM 复制公钥到 dist
copy public_key.pem dist\

echo ========================================
echo 打包完成！
echo 输出目录: dist\
echo ========================================
pause
```

使用：
```bash
build.bat
```

---

## 🎁 发布清单

发给客户前检查：

- [ ] `RTSP监测系统.exe`（或 `main.exe`）
- [ ] `license.dat`（为该客户生成的）
- [ ] `public_key.pem`
- [ ] `使用说明.txt`（可选）
- [ ] **确认没有** `private_key.pem`
- [ ] **确认没有** `generate_license.py` 或 `generate_license.exe`

---

## 💡 总结

1. **可以直接打包**：当前代码无需修改即可打包
2. **许可证文件必须外部**：`license.dat` 和 `public_key.pem` 不能打包进 exe
3. **推荐 --onedir 模式**：兼容性更好，误报率更低
4. **测试很重要**：在干净的环境测试打包结果

**打包后的许可证系统和 Python 版本功能完全一致！**
