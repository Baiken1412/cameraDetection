# RTSP监测系统 - 打包文件清单

## 📋 必须放在 EXE 外部的文件和目录

根据你的项目分析，以下文件/目录**必须**放在 exe 外部：

---

### 1. ⚙️ 配置文件（必须外部）

```
config.py                    ← 数据库配置、路径配置等
```

**原因：**
- 包含数据库密码、IP地址等敏感信息
- 不同客户的配置不同（数据库地址、图片保存路径等）
- 需要客户能够修改配置

**打包方式：**
```bash
# 不要用 --add-data 打包，让它保持外部
pyinstaller --onefile main.py
# config.py 会自动作为外部文件被引用
```

---

### 2. 🔐 许可证文件（必须外部）

```
license.dat                  ← 许可证文件
public_key.pem              ← 公钥文件
```

**原因：**
- 每个客户的许可证不同
- 需要能够更换/更新许可证

---

### 3. 🤖 模型文件（必须外部）

```
models/
├── yolo11n.onnx            ← YOLO模型文件（10MB+）
├── yolo11n.pt              ← PyTorch模型
└── yolov8m.pt              ← 备用模型
```

**原因：**
- 模型文件很大（10MB+），打包进exe会很臃肿
- 可能需要更新模型
- 代码中使用相对路径 `'models'` 读取

**打包方式：**
```bash
# 使用 --add-data 将整个 models 目录打包
pyinstaller --onefile --add-data "models;models" main.py

# 或者不打包，作为外部目录（推荐）
```

---

### 4. 📝 日志目录（必须外部）

```
logs/
└── rtsp_monitor.log        ← 运行日志
```

**原因：**
- 运行时需要写入日志
- exe 内部无法写入文件
- 配置文件中指定：`'file': 'logs/rtsp_monitor.log'`

**打包方式：**
```bash
# 不打包，程序运行时自动创建
# config.py 中的 ensure_directories() 会自动创建 logs 目录
```

---

### 5. 📁 数据目录（必须外部）

```
data/
├── cropped_persons/        ← 裁剪后的人员图片
├── features/               ← 特征文件
├── input_images/           ← 输入图片
└── results/                ← 结果
```

**原因：**
- 运行时需要读写数据
- 配置文件中指定：`'input_image_dir': 'data/input_images'` 等

**打包方式：**
```bash
# 不打包，程序运行时需要
# 可以在发布时创建空目录
```

---

### 6. 🖼️ 图片保存目录（客户自定义）

```
D:/ruoyi/uploadPath/caseapp/    ← 图片保存路径（配置文件中指定）
```

**原因：**
- 配置文件中指定：`'image_save_path': 'D:/ruoyi/uploadPath/caseapp'`
- 客户可能需要修改路径
- 运行时需要写入图片

**打包方式：**
```bash
# 不打包，由客户在 config.py 中配置
# 程序运行时自动创建（如果不存在）
```

---

## 📦 推荐的打包命令

### 方案A：最小打包（推荐）

**所有配置、模型、数据都在外部，最灵活**

```bash
pyinstaller --onefile --name "RTSP监测系统" main.py
```

**发布目录结构：**
```
RTSP监测系统/
├── RTSP监测系统.exe        ← 打包后的exe
├── config.py               ← 配置文件
├── license.dat             ← 许可证
├── public_key.pem          ← 公钥
├── models/                 ← 模型目录
│   ├── yolo11n.onnx
│   ├── yolo11n.pt
│   └── yolov8m.pt
├── logs/                   ← 日志目录（自动创建）
├── data/                   ← 数据目录
│   ├── cropped_persons/
│   ├── features/
│   ├── input_images/
│   └── results/
└── 使用说明.txt
```

---

### 方案B：半打包（模型打包进exe）

**模型打包进exe，配置和数据在外部**

```bash
pyinstaller --onefile ^
    --name "RTSP监测系统" ^
    --add-data "models;models" ^
    main.py
```

**优点：** 模型不会丢失
**缺点：** exe 文件很大（20MB+），无法更新模型

**发布目录结构：**
```
RTSP监测系统/
├── RTSP监测系统.exe        ← 打包后的exe（包含models）
├── config.py               ← 配置文件
├── license.dat             ← 许可证
├── public_key.pem          ← 公钥
├── logs/                   ← 日志目录（自动创建）
├── data/                   ← 数据目录
└── 使用说明.txt
```

---

## 🎯 完整打包流程

### 第1步：准备发布目录

```bash
# 创建发布目录
mkdir release
cd release
mkdir RTSP监测系统
```

### 第2步：打包exe

```bash
# 回到项目目录
cd ..

# 打包（方案A：最小打包）
pyinstaller --onefile --name "RTSP监测系统" main.py
```

### 第3步：复制必要文件

```bash
# 复制exe
copy dist\RTSP监测系统.exe release\RTSP监测系统\

# 复制配置文件
copy config.py release\RTSP监测系统\

# 复制许可证文件
copy public_key.pem release\RTSP监测系统\
# license.dat 为每个客户单独生成

# 复制模型目录
xcopy models release\RTSP监测系统\models\ /E /I

# 创建空的数据目录
mkdir release\RTSP监测系统\data
mkdir release\RTSP监测系统\data\cropped_persons
mkdir release\RTSP监测系统\data\features
mkdir release\RTSP监测系统\data\input_images
mkdir release\RTSP监测系统\data\results
```

### 第4步：为客户生成许可证

```bash
# 客户提供机器码后
python generate_license.py --create --machine-code <客户机器码> --days 365

# 复制并改名
copy license_xxxxxxxx.dat release\RTSP监测系统\license.dat
```

### 第5步：编辑配置文件

打开 `release\RTSP监测系统\config.py`，修改客户的配置：

```python
# 数据库配置（根据客户环境修改）
DATABASE_CONFIG = {
    'host': '127.0.0.1',      # 客户的数据库地址
    'port': 3306,
    'user': 'root',
    'password': '客户的密码',
    'database': 'caseappdb',
    'charset': 'utf8mb4'
}

# 图片保存路径（根据客户环境修改）
'image_save_path': 'D:/ruoyi/uploadPath/caseapp',  # 客户的路径
```

### 第6步：打包发送

```bash
# 压缩整个目录
# 可以用 7-Zip、WinRAR 等压缩为 RTSP监测系统.zip
```

---

## 📄 发给客户的完整清单

### 必须包含的文件

- [x] `RTSP监测系统.exe`
- [x] `config.py`（已根据客户环境配置好）
- [x] `license.dat`（为客户生成的许可证）
- [x] `public_key.pem`
- [x] `models/`（包含所有模型文件）
- [x] `data/`（空目录结构）
- [x] `使用说明.txt`

### 客户需要准备的

1. **MySQL数据库**
   - 已创建数据库 `caseappdb`
   - 已导入数据库表结构

2. **图片保存目录**
   - 确保 `D:/ruoyi/uploadPath/caseapp` 目录存在
   - 或者修改 `config.py` 中的路径

3. **网络环境**
   - 能访问 RTSP 摄像头
   - 能访问数据库

---

## ⚠️ 重要注意事项

### 1. config.py 的处理

**问题：** `config.py` 是 Python 源代码，打包后需要外部文件

**解决方案A：保持 .py 格式（推荐）**
- 客户需要安装 Python（或者你提供精简版Python）
- 优点：客户可以直接修改配置
- 缺点：客户可能修改错误

**解决方案B：改为 .ini 或 .json 格式**
- 修改代码，使用 configparser 或 json 读取配置
- 优点：客户更容易修改，不会语法错误
- 缺点：需要修改代码

**解决方案C：打包进exe，提供配置工具**
- 将 config.py 打包进 exe
- 提供一个配置工具（GUI）让客户修改
- 优点：最专业
- 缺点：需要额外开发

### 2. 模型文件的处理

**当前大小：**
- `yolo11n.onnx`: 10.7 MB
- 其他模型: < 1 KB

**建议：** 使用方案A（外部），方便更新模型

### 3. 日志和数据目录

程序会自动创建，不需要手动创建（`ensure_directories()` 函数）

---

## 🧪 测试打包结果

### 测试清单

1. [ ] 在没有 Python 的电脑上运行
2. [ ] 许可证验证正常
3. [ ] 能连接数据库
4. [ ] 能读取模型文件
5. [ ] 能保存图片
6. [ ] 能写入日志
7. [ ] 能连接 RTSP 摄像头

### 测试步骤

```bash
# 1. 复制发布目录到测试电脑
# 2. 修改 config.py（数据库配置）
# 3. 运行
RTSP监测系统.exe

# 4. 检查是否正常启动
# 5. 检查日志文件是否生成
```

---

## 📊 文件大小估算

```
RTSP监测系统.exe          ~50-80 MB（包含所有Python依赖）
config.py                  ~6 KB
license.dat               ~1 KB
public_key.pem            ~1 KB
models/                   ~11 MB
---------------------------------
总计                      ~60-95 MB
```

压缩后约 30-40 MB。

---

## 🔄 更新策略

### 软件更新
只需替换 `RTSP监测系统.exe`

### 配置更新
修改 `config.py` 后重启程序

### 模型更新
替换 `models/` 目录中的模型文件

### 许可证更新
替换 `license.dat` 文件

---

## 总结

**必须外部的文件：**
1. ✅ config.py（配置文件）
2. ✅ license.dat（许可证）
3. ✅ public_key.pem（公钥）
4. ✅ models/（模型文件，10MB+）
5. ✅ logs/（日志目录，运行时创建）
6. ✅ data/（数据目录，运行时使用）

**可以打包的：**
- ❌ models/（如果选择方案B，但不推荐）

**推荐：** 使用方案A（最小打包），所有配置、模型、数据都在外部，最灵活！
