 已完成两处修改：

  修改内容

  1. image_storage.py (行136-141)

  文件名增加毫秒精度
  # 修改前
  timestamp = actual_time.strftime('%Y%m%d%H%M%S')
  # 文件名示例：29_摄像头_20250130100005.jpg

  # 修改后
  timestamp = actual_time.strftime('%Y%m%d%H%M%S') + f"_{actual_time.microsecond // 1000:03d}"
  # 文件名示例：29_摄像头_20250130100005_123.jpg

  2. database.py (行260-306)

  数据库去重检查
  - 插入前先查询是否存在相同 pstp（图片路径）+ qyid（摄像头ID）的记录
  - 如果存在：更新 jscs（检测次数）+1，更新 jssj（结束时间），返回已有记录ID
  - 如果不存在：正常插入新记录
  - 增加了警告日志，方便排查重复问题

  效果
  ┌──────────────────┬──────────────────────┬────────────────────────┐
  │       场景       │        修改前        │         修改后         │
  ├──────────────────┼──────────────────────┼────────────────────────┤
  │ 同秒内多次保存   │ 文件覆盖，多条DB记录 │ 文件名不同，各自独立   │
  ├──────────────────┼──────────────────────┼────────────────────────┤
  │ 相同图片重复入库 │ 创建多条重复记录     │ 合并到已有记录，jscs+1 │
  └──────────────────┴──────────────────────┴────────────────────────┘
  重启服务后生效。


   在公司测试，当把"min_change_area": 150时，好像有些摄像头获取的监测数量少；但我改到30时，监测多了，但数据有重复，即在硬盘存了一个照片，但是在数据库的轨迹表里记了很多条，但都是指向一个图片地址。当把"min_change_area"改为150时，重复的数据又不再出现了。

   ● min_change_area 参数详解

  参数作用

  min_change_area 是在背景建模检测中，过滤掉小于该面积（像素数）的变化区域。

  在 image_detection.py:194-216 中：

  def _filter_small_areas(self, thresh):
      if self.min_change_area > 0:
          contours, _ = cv2.findContours(thresh, ...)
          for contour in contours:
              area = cv2.contourArea(contour)
              if area >= self.min_change_area:  # 只保留大于阈值的区域
                  cv2.drawContours(thresh_filtered, [contour], -1, 255, -1)

  不同取值的对比
  ┌───────┬────────────┬────────┬──────────────────────┐
  │ 取值  │ 大约相当于 │ 敏感度 │       适用场景       │
  ├───────┼────────────┼────────┼──────────────────────┤
  │ 30    │ 5×6 像素   │ 极高   │ 微小物体、远距离检测 │
  ├───────┼────────────┼────────┼──────────────────────┤
  │ 150   │ 12×12 像素 │ 高     │ 中远距离人员         │
  ├───────┼────────────┼────────┼──────────────────────┤
  │ 500   │ 22×22 像素 │ 中等   │ 近距离人员（推荐）   │
  ├───────┼────────────┼────────┼──────────────────────┤
  │ 1000  │ 32×32 像素 │ 低     │ 只检测大面积运动     │
  ├───────┼────────────┼────────┼──────────────────────┤
  │ 2000+ │ 45×45 像素 │ 很低   │ 过滤绝大部分干扰     │
  └───────┴────────────┴────────┴──────────────────────┘
  实际像素参考（以 1920×1080 分辨率为例）

  画面总像素：1920 × 1080 = 2,073,600 像素

  一个正常成年人在画面中的占比：
  - 近距离（占画面 1/4 高度）：约 270×500 = 135,000 像素
  - 中距离（占画面 1/8 高度）：约 135×250 = 33,750 像素
  - 远距离（占画面 1/16 高度）：约 67×125 = 8,375 像素
  - 很远（占画面 1/32 高度）：约 34×62 = 2,108 像素

  不同值的表现差异

  min_change_area = 30（极敏感）

  ✅ 优点：
  - 能检测到很远的人（画面中很小的目标）
  - 不会漏检

  ❌ 缺点：
  - 噪声误报多（光线闪烁、树叶晃动、摄像头抖动）
  - 检测频率过高，系统负载大
  - 容易触发你遇到的"重复记录"问题

  min_change_area = 150（较敏感）

  ✅ 优点：
  - 能检测中远距离的人
  - 比 30 稳定很多

  ❌ 缺点：
  - 仍可能有一些噪声误报
  - 检测频率较高

  min_change_area = 500（平衡，当前配置）

  ✅ 优点：
  - 过滤掉大部分噪声（光线变化、小物体）
  - 检测频率适中
  - 系统稳定

  ❌ 缺点：
  - 可能漏检很远的小目标
  - 人员刚进入画面边缘时可能检测不到

  min_change_area = 1000+（保守）

  ✅ 优点：
  - 几乎没有噪声误报
  - 系统非常稳定

  ❌ 缺点：
  - 只能检测近距离大目标
  - 远距离人员容易漏检

  调参技巧

  1. 根据摄像头距离调整

  走廊/门口（近距离）：500-1000
  大厅/房间（中距离）：200-500
  广场/停车场（远距离）：50-200

  2. 根据环境干扰调整

  室内稳定光源：可以用较小值（100-300）
  室外/窗边（光线变化大）：用较大值（500-1000）
  有风/树叶晃动：用较大值（800+）

  3. 配合其他参数使用
  ┌──────────────────────────────┬──────────────┬──────────────────────────────────────┐
  │             参数             │     作用     │               配合建议               │
  ├──────────────────────────────┼──────────────┼──────────────────────────────────────┤
  │ change_threshold             │ 前景比例阈值 │ min_change_area 小时，可适当提高此值 │
  ├──────────────────────────────┼──────────────┼──────────────────────────────────────┤
  │ consecutive_frames_threshold │ 连续帧确认   │ 设为 2-3，减少瞬时噪声               │
  ├──────────────────────────────┼──────────────┼──────────────────────────────────────┤
  │ morphology_operation         │ 形态学去噪   │ 设为 1（开运算），去除小噪点         │
  ├──────────────────────────────┼──────────────┼──────────────────────────────────────┤
  │ bg_var_threshold             │ 背景模型方差 │ 值越大越不敏感                       │
  └──────────────────────────────┴──────────────┴──────────────────────────────────────┘
  4. 推荐配置组合

  高敏感度（远距离检测）
  {
    "min_change_area": 100,
    "change_threshold": 0.03,
    "consecutive_frames_threshold": 3,
    "morphology_operation": 1
  }

  平衡模式（推荐）
  {
    "min_change_area": 500,
    "change_threshold": 0.02,
    "consecutive_frames_threshold": 2,
    "morphology_operation": 1
  }

  高稳定性（噪声环境）
  {
    "min_change_area": 1000,
    "change_threshold": 0.02,
    "consecutive_frames_threshold": 2,
    "morphology_operation": 3
  }

  你的问题场景

  当你把 min_change_area 从 500 调到 30 时：

  1. 检测灵敏度提升 16 倍以上（500/30 ≈ 16.7）
  2. 大量噪声被当作"变化"，触发频繁检测
  3. VideoStreamReader 时间戳滞后问题被放大
  4. 最终导致重复记录

  建议：如果需要更敏感的检测，可以逐步调整（500 → 300 → 200），而不是直接跳到 30。同时配合提高 consecutive_frames_threshold 到 3。
