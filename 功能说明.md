# cameraDetection 功能说明文档

## 一、系统概述

**cameraDetection** 是一套基于深度学习的 RTSP 视频流实时人员检测监测系统，适用于建筑工地、园区、商场等需要实时人员监控和行为分析的场景。

### 1.1 系统架构

```
┌─────────────────────────────────────────────────────────────────┐
│                        主程序 (main.py)                          │
├─────────────────────────────────────────────────────────────────┤
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐              │
│  │ 摄像头监测1  │  │ 摄像头监测2  │  │ 摄像头监测N  │  ...         │
│  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘              │
│         │                │                │                      │
│         └────────────────┼────────────────┘                      │
│                          ▼                                       │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │                    YOLO 实例池                              │  │
│  │   ┌────────┐  ┌────────┐  ┌────────┐                      │  │
│  │   │ YOLO 1 │  │ YOLO 2 │  │ YOLO N │  (共享/独立模式)      │  │
│  │   └────────┘  └────────┘  └────────┘                      │  │
│  └───────────────────────────────────────────────────────────┘  │
│                          │                                       │
│         ┌────────────────┼────────────────┐                      │
│         ▼                ▼                ▼                      │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐              │
│  │  图片存储    │  │  数据库      │  │  Web API    │              │
│  └─────────────┘  └─────────────┘  └─────────────┘              │
└─────────────────────────────────────────────────────────────────┘
```

### 1.2 技术栈

| 组件 | 技术 |
|------|------|
| 深度学习框架 | YOLOv8 / YOLOv11 |
| 推理引擎 | OpenVINO (Intel) / ONNX Runtime (AMD/通用) |
| 视频处理 | OpenCV, PyAV |
| 数据库 | MySQL + PyMySQL连接池 |
| Web框架 | Flask |
| 日志 | Loguru |

---

## 二、核心功能模块

### 2.1 视频流采集模块

**功能描述**: 从 RTSP 摄像头或 USB 摄像头实时采集视频流。

**文件**: `video_stream_reader.py`, `input_shaper.py`

**特性**:
- 支持 RTSP 协议视频流
- 支持 USB 本地摄像头
- 独立线程读取，避免主线程阻塞
- 自动缓冲区管理，保证实时性
- 断线自动重连机制

**配置参数**:
```python
RTSP_MONITOR_CONFIG = {
    'use_video_stream_reader': True,   # 使用独立线程读取
    'buffer_flush_interval': 10,        # 缓冲区清理间隔(秒)
    'reconnect_interval': 5,            # 断线重连间隔(秒)
}
```

---

### 2.2 输入整形层

**功能描述**: 将不规则的 RTSP 视频流转换为稳定帧率输出，确保检测负载可控。

**文件**: `input_shaper.py`

**特性**:
- 环形缓冲区管理
- 目标帧率控制
- 丢帧策略(保新弃旧)
- 时间戳传递保证

**配置参数**:
```python
INPUT_SHAPER_CONFIG = {
    'enabled': True,
    'target_fps': 5,           # 目标输出帧率
    'buffer_size': 30,         # 缓冲区大小
    'drop_policy': 'newest',   # 丢帧策略
}
```

**应用场景**:
- RTSP 源帧率不稳定
- 需要固定检测频率
- 多摄像头资源规划

---

### 2.3 变化检测模块

**功能描述**: 基于背景建模算法检测画面变化，只有检测到变化时才触发人员检测，节省计算资源。

**文件**: `image_detection.py`

**算法**:
- MOG2 (混合高斯模型)
- KNN (K近邻)

**特性**:
- 背景自适应学习
- 前景比例计算
- 连续帧确认机制(减少误报)
- 形态学操作降噪
- 最小变化区域过滤

**配置参数**:
```python
IMAGE_DETECTION_CONFIG = {
    'bg_subtractor_type': 'MOG2',      # 背景减除算法
    'change_threshold': 0.02,           # 变化阈值(2%)
    'min_change_area': 500,             # 最小变化面积(像素)
    'consecutive_frames': 2,            # 连续帧阈值
    'learning_rate': 0.001,             # 背景学习率
}
```

**工作流程**:
```
输入帧 → 背景减除 → 前景提取 → 形态学处理 → 面积计算 → 阈值判断 → 是否触发检测
```

---

### 2.4 人员检测模块

**功能描述**: 使用 YOLO 深度学习模型检测画面中的人员，返回边界框和置信度。

**文件**: `core/person_detector_adaptive.py`, `core/detector.py`, `core/detector_onnx.py`, `core/detector_openvino.py`

**支持模型**:
- YOLOv8n / YOLOv8s / YOLOv8m
- YOLOv11n / YOLOv11s

**推理引擎自适应**:
| CPU类型 | 推理引擎 | 优化特性 |
|--------|---------|---------|
| Intel | OpenVINO | AVX-512, INT8量化 |
| AMD | ONNX Runtime | 多线程优化 |
| 其他 | ONNX Runtime | 通用优化 |

**配置参数**:
```python
ADAPTIVE_DETECTION_CONFIG = {
    'enabled': True,
    'conf_threshold': 0.5,     # 置信度阈值
    'iou_threshold': 0.4,      # NMS IOU阈值
    'force_engine': None,      # 强制引擎: 'openvino'/'onnx'/None(自动)
    'num_threads': None,       # 推理线程数(None=自动)
    'model_path': 'yolov11n.onnx',
}
```

**输出格式**:
```python
[
    {'box': [x1, y1, x2, y2], 'confidence': 0.95, 'class': 'person'},
    {'box': [x1, y1, x2, y2], 'confidence': 0.87, 'class': 'person'},
]
```

---

### 2.5 YOLO 实例池

**功能描述**: 管理多个 YOLO 检测器实例，支持多摄像头共享，减少内存占用。

**文件**: `core/yolo_pool.py`, `core/yolo_process_pool.py`

**两种实现**:

| 类型 | 文件 | 特点 | 适用场景 |
|------|------|------|---------|
| 多线程池 | `yolo_pool.py` | 内存共享,简单 | 一般场景 |
| 多进程池 | `yolo_process_pool.py` | 无GIL限制,高吞吐 | 高并发场景 |

**池分配模式**:
```
数据库字段 yolo_pool_id:
├── NULL 或 -1  → 使用全局默认池(所有摄像头共享)
├── 0           → 独立YOLO实例(不共享)
└── 1, 2, 3...  → 使用指定ID的池(相同ID共享)
```

**配置参数**:
```python
YOLO_POOL_CONFIG = {
    'enabled': True,
    'pool_size': 2,              # 默认池实例数
    'detector_type': 'adaptive', # 检测器类型
    'timeout': 30,               # 借用超时(秒)
    'use_multiprocess': False,   # 是否使用多进程池
}
```

**内存优化效果**:
```
10路摄像头:
├── 无池(独立实例): ~5GB 内存
└── 使用池(2实例):  ~1GB 内存
```

---

### 2.6 单摄像头监测模块

**功能描述**: 管理单个摄像头的完整检测流程，包括视频采集、变化检测、人员识别、结果存储。

**文件**: `camera_monitor.py`

**主要功能**:
- 视频流采集与管理
- 变化检测触发
- YOLO 人员检测调用
- 检测结果处理
- 轨迹生成与合并
- 异常处理与重连

**配置参数**:
```python
RTSP_MONITOR_CONFIG = {
    'sample_interval': 1,              # 采样间隔(秒)
    'trajectory_merge_interval': 60,   # 轨迹合并时间窗口(秒)
    'min_record_interval': 10,         # 最小记录间隔(秒)
    'max_retry_count': 3,              # 最大重试次数
}
```

**检测模式**:
| 模式 | 检测频率 | 触发条件 |
|------|---------|---------|
| 快速模式 | 1秒/次 | 检测到变化时 |
| 慢速模式 | 10秒/次 | 无变化时 |

---

### 2.7 图片存储模块

**功能描述**: 保存检测到的人员截图，支持文件系统和 Base64 两种存储方式。

**文件**: `image_storage.py`

**存储方式**:
| 方式 | 描述 | 配置 |
|------|------|------|
| 文件系统 | 保存到指定目录 | `storage_mode: 'file'` |
| Base64 | 编码后存数据库 | `storage_mode: 'base64'` |

**文件命名规则**:
```
{camera_id}_{timestamp}.jpg
示例: CAM001_20260126_143052.jpg
```

**配置参数**:
```python
IMAGE_STORAGE_CONFIG = {
    'storage_mode': 'file',
    'base_path': 'D:/appdata/uploadPath/caseapp/',
    'image_quality': 95,        # JPEG质量
    'max_width': 1920,          # 最大宽度
}
```

---

### 2.8 数据库模块

**功能描述**: 管理 MySQL 数据库连接，存储摄像头配置和检测记录。

**文件**: `database.py`

**主要功能**:
- 连接池管理
- 摄像头配置读取
- 检测记录存储
- 轨迹合并处理
- 状态更新

**连接池配置**:
```python
DATABASE_CONFIG = {
    'host': 'localhost',
    'port': 3306,
    'user': 'root',
    'password': '******',
    'database': 'caseappdb',
    'pool_size': 5,
    'max_connections': 20,
}
```

**主要数据表**:
| 表名 | 用途 |
|------|------|
| 摄像头配置表 | 存储RTSP地址、池ID等 |
| 检测记录表 | 存储检测时间、人数、图片等 |
| 轨迹表 | 存储人员轨迹信息 |

**核心方法**:
```python
get_all_cameras()           # 获取所有摄像头配置
save_detection_record()     # 保存检测记录
merge_trajectory()          # 合并轨迹
update_record_status()      # 更新记录状态
```

---

### 2.9 配置管理模块

**功能描述**: 管理系统配置，支持 Python 配置和 JSON 配置文件。

**文件**: `config.py`, `config_loader.py`, `config.json`

**配置层级**:
```
config.json (用户自定义,优先)
    ↓
config.py (默认配置,回退)
```

**主要配置项**:
| 配置模块 | 说明 |
|---------|------|
| RTSP_MONITOR_CONFIG | 视频监测参数 |
| ADAPTIVE_DETECTION_CONFIG | 自适应检测参数 |
| YOLO_POOL_CONFIG | YOLO池配置 |
| INPUT_SHAPER_CONFIG | 输入整形层配置 |
| DATABASE_CONFIG | 数据库连接 |
| IMAGE_STORAGE_CONFIG | 图片存储配置 |
| LOG_CONFIG | 日志配置 |

---

### 2.10 许可证管理模块

**功能描述**: 管理系统许可证，控制功能授权和有效期。

**文件**: `license_manager.py`, `generate_license.py`

**功能**:
- 许可证生成
- 许可证验证
- 有效期检查
- 功能限制

---

### 2.11 Web API 模块

**功能描述**: 提供 RESTful API 接口，用于管理和查询检测数据。

**文件**: `person_management_api.py`

**主要接口**:
| 接口 | 方法 | 功能 |
|------|------|------|
| /api/cameras | GET | 获取摄像头列表 |
| /api/detections | GET | 获取检测记录 |
| /api/detections/{id} | GET | 获取单条记录 |
| /api/confirm | POST | 手动确认记录 |
| /api/statistics | GET | 获取统计数据 |

**启动方式**:
```bash
python person_management_api.py
# 或
flask run --host=0.0.0.0 --port=5000
```

---

### 2.12 ReID 人员重识别模块

**功能描述**: 基于特征提取的人员重识别，支持跨摄像头人员追踪。

**文件**: `core/feature_extractor.py`, `core/similarity.py`, `reid_integration.py`

**功能**:
- 人员特征提取
- 特征相似度计算
- 跨摄像头人员匹配
- 特征数据库管理

---

## 三、系统工作流程

### 3.1 启动流程

```
1. 系统初始化
   ├── 加载配置文件
   ├── 初始化日志系统
   ├── 验证许可证
   └── 创建数据库连接池

2. 创建YOLO池
   ├── 查询摄像头配置
   ├── 分析池需求(全局/分组/独立)
   └── 创建必要的YOLO实例池

3. 启动摄像头监测
   ├── 为每个摄像头创建CameraMonitor
   ├── 分配YOLO池
   └── 启动监测线程

4. 主循环
   ├── 监控线程状态
   ├── 处理系统信号
   └── 资源管理
```

### 3.2 检测流程

```
视频帧输入
    │
    ▼
┌─────────────────┐
│ 背景建模变化检测 │ ←── 无变化 → 继续监测
└────────┬────────┘
         │ 有变化
         ▼
┌─────────────────┐
│ 从YOLO池借用实例 │ ←── 超时 → 记录日志,跳过
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ YOLO人员检测    │ ←── 无人 → 归还实例,继续
└────────┬────────┘
         │ 检测到人
         ▼
┌─────────────────┐
│ 裁剪人员区域    │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ 保存检测图片    │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ 生成/更新轨迹   │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ 存储到数据库    │
└─────────────────┘
```

---

## 四、部署与配置

### 4.1 系统要求

| 项目 | 最低配置 | 推荐配置 |
|------|---------|---------|
| CPU | 4核 | 8核+ (Intel推荐) |
| 内存 | 4GB | 8GB+ |
| 硬盘 | 50GB | 100GB+ SSD |
| 操作系统 | Windows 10 / Ubuntu 20.04 | Windows Server / Ubuntu 22.04 |
| Python | 3.8+ | 3.10+ |

### 4.2 依赖安装

```bash
pip install -r requirements.txt
```

**核心依赖**:
```
pymysql>=1.1.0
opencv-python>=4.8.0
av>=8.0.0
numpy>=1.24.0
Pillow>=10.0.0
loguru>=0.7.0
Flask>=2.3.0
ultralytics
openvino (可选,Intel CPU)
onnxruntime
```

### 4.3 配置文件

编辑 `config.json`:
```json
{
  "database": {
    "host": "localhost",
    "port": 3306,
    "user": "root",
    "password": "your_password",
    "database": "caseappdb"
  },
  "yolo_pool": {
    "enabled": true,
    "pool_size": 2
  },
  "detection": {
    "conf_threshold": 0.5,
    "iou_threshold": 0.4
  }
}
```

### 4.4 启动命令

```bash
# 启动主程序
python main.py

# 启动Web API
python person_management_api.py

# 检查CPU类型
python check_cpu.py

# 检查CUDA支持
python check_cuda.py
```

---

## 五、性能指标

### 5.1 检测性能

| 指标 | 数值 |
|------|------|
| 单帧检测延迟 | 200-500ms |
| 10路摄像头总帧率 | >10 fps |
| 检测准确率 | >90% |
| 误报率 | <5% |

### 5.2 资源占用

| 配置 | 内存占用 | CPU占用 |
|------|---------|---------|
| 单摄像头+独立YOLO | ~1GB | ~30% |
| 10摄像头+YOLO池(2实例) | ~2GB | ~60% |
| 10摄像头+独立YOLO | ~5GB | ~80% |

---

## 六、日志与监控

### 6.1 日志配置

```python
LOG_CONFIG = {
    'level': 'INFO',
    'format': '{time} | {level} | {message}',
    'rotation': '100 MB',
    'retention': '7 days',
    'path': 'logs/',
}
```

### 6.2 日志级别

| 级别 | 用途 |
|------|------|
| DEBUG | 详细调试信息 |
| INFO | 常规运行信息 |
| WARNING | 警告信息 |
| ERROR | 错误信息 |

---

## 七、常见问题

### Q1: RTSP连接失败
**解决**: 检查RTSP地址格式，确认摄像头在线，检查网络连通性。

### Q2: 检测延迟高
**解决**: 增加YOLO池实例数，使用更小的模型(yolov11n)，检查CPU性能。

### Q3: 内存占用高
**解决**: 使用YOLO池共享实例，减少pool_size，限制同时监测的摄像头数量。

### Q4: 误报率高
**解决**: 提高置信度阈值，调整变化检测参数，增加连续帧确认数。

---

## 八、版本历史

| 版本 | 日期 | 更新内容 |
|------|------|---------|
| 1.0 | - | 初始版本 |
| - | - | 添加InputShaper输入整形层 |
| - | - | 添加YOLO实例池 |
| - | - | 自适应推理引擎选择 |

---

*文档版本: 1.0*
*生成日期: 2026-01-26*
