# cameraDetection 测试功能文档

## 一、项目概述

**cameraDetection** 是一个 RTSP 视频流实时人员检测监测系统，主要功能包括：

- 实时 RTSP 视频流采集与监测
- 基于 YOLOv8/v11 的人员检测
- 背景建模法变化检测
- 检测结果存储到 MySQL 数据库
- 多摄像头支持与 YOLO 实例池资源共享
- Web API 管理界面

---

## 二、核心模块功能清单

| 模块 | 文件 | 功能描述 |
|------|------|----------|
| 主程序入口 | `main.py` | 系统启动、多摄像头协调、YOLO池管理 |
| 单摄像头监测 | `camera_monitor.py` | 视频采集、变化检测、人员识别、轨迹记录 |
| 变化检测 | `image_detection.py` | 背景建模(MOG2/KNN)、前景检测、连续帧确认 |
| 人员检测器 | `core/person_detector_adaptive.py` | 自适应推理引擎选择、YOLOv11推理 |
| YOLO实例池 | `core/yolo_pool.py` | 多线程YOLO实例管理、负载均衡 |
| YOLO进程池 | `core/yolo_process_pool.py` | 多进程YOLO实例管理(无GIL限制) |
| 视频流读取 | `video_stream_reader.py` | 独立线程读取RTSP流、实时缓冲 |
| 输入整形层 | `input_shaper.py` | 帧率稳定、环形缓冲区管理 |
| 图片存储 | `image_storage.py` | 检测图片保存(文件/Base64) |
| 数据库模块 | `database.py` | MySQL连接池、记录存储、轨迹合并 |
| 配置管理 | `config.py`, `config_loader.py` | 配置加载与管理 |
| 许可证管理 | `license_manager.py` | 许可证验证与管理 |
| Web API | `person_management_api.py` | Flask REST API管理界面 |

---

## 三、测试功能点

### 3.1 单元测试功能点

#### 3.1.1 变化检测模块 (`image_detection.py`)

| 测试编号 | 测试功能点 | 测试描述 | 预期结果 |
|---------|-----------|---------|---------|
| UT-CD-001 | 背景建模初始化 | 测试 MOG2/KNN 背景减除器初始化 | 成功创建背景减除器实例 |
| UT-CD-002 | 变化阈值检测 | 输入有明显变化的帧，检测是否触发 | 前景比例超过阈值时返回 True |
| UT-CD-003 | 无变化检测 | 输入无变化的连续帧 | 返回 False，不触发检测 |
| UT-CD-004 | 连续帧确认机制 | 测试连续帧阈值设置 | 只有连续N帧都有变化才触发 |
| UT-CD-005 | 形态学操作 | 测试噪声过滤效果 | 小噪点被正确过滤 |
| UT-CD-006 | 最小变化区域 | 测试最小变化面积阈值 | 小于阈值的变化被忽略 |

#### 3.1.2 人员检测模块 (`core/person_detector_adaptive.py`)

| 测试编号 | 测试功能点 | 测试描述 | 预期结果 |
|---------|-----------|---------|---------|
| UT-PD-001 | 检测器初始化 | 测试自适应检测器创建 | 根据CPU类型选择正确引擎 |
| UT-PD-002 | 人员检测-有人 | 输入包含人员的图像 | 返回正确的边界框和置信度 |
| UT-PD-003 | 人员检测-无人 | 输入无人员的图像 | 返回空列表 |
| UT-PD-004 | 置信度阈值 | 测试不同置信度阈值 | 低于阈值的检测被过滤 |
| UT-PD-005 | NMS非极大值抑制 | 测试重叠框过滤 | 重叠框被正确合并 |
| UT-PD-006 | 多人检测 | 输入多人场景图像 | 正确检测所有人员 |

#### 3.1.3 YOLO实例池 (`core/yolo_pool.py`)

| 测试编号 | 测试功能点 | 测试描述 | 预期结果 |
|---------|-----------|---------|---------|
| UT-YP-001 | 池初始化 | 测试指定数量实例创建 | 创建正确数量的YOLO实例 |
| UT-YP-002 | 实例借用 | 测试从池中获取实例 | 成功获取可用实例 |
| UT-YP-003 | 实例归还 | 测试实例归还到池 | 实例正确归还并可再次使用 |
| UT-YP-004 | 等待超时 | 测试所有实例占用时的等待 | 超时后抛出异常或返回None |
| UT-YP-005 | 线程安全 | 多线程并发借用/归还 | 无竞态条件，数据一致 |
| UT-YP-006 | 池统计信息 | 测试使用统计功能 | 正确返回借用次数、等待时间等 |

#### 3.1.4 视频流读取 (`video_stream_reader.py`)

| 测试编号 | 测试功能点 | 测试描述 | 预期结果 |
|---------|-----------|---------|---------|
| UT-VR-001 | 流连接 | 测试RTSP流连接 | 成功建立连接 |
| UT-VR-002 | 帧读取 | 测试连续帧读取 | 正确返回视频帧 |
| UT-VR-003 | 最新帧获取 | 测试获取最新帧功能 | 返回缓冲区中最新帧 |
| UT-VR-004 | 断线重连 | 模拟连接中断 | 自动重连成功 |
| UT-VR-005 | 线程停止 | 测试优雅停止 | 线程正确退出，资源释放 |

#### 3.1.5 输入整形层 (`input_shaper.py`)

| 测试编号 | 测试功能点 | 测试描述 | 预期结果 |
|---------|-----------|---------|---------|
| UT-IS-001 | 帧率稳定 | 测试输出帧率稳定性 | 输出帧率接近目标帧率 |
| UT-IS-002 | 环形缓冲区 | 测试缓冲区管理 | 正确的FIFO行为 |
| UT-IS-003 | 丢帧策略 | 测试帧溢出处理 | 保新弃旧，丢弃旧帧 |
| UT-IS-004 | 时间戳传递 | 测试帧时间戳一致性 | 时间戳正确传递 |

#### 3.1.6 图片存储 (`image_storage.py`)

| 测试编号 | 测试功能点 | 测试描述 | 预期结果 |
|---------|-----------|---------|---------|
| UT-ST-001 | 文件保存 | 测试图片文件保存 | 正确保存到指定路径 |
| UT-ST-002 | 文件命名 | 测试文件命名规则 | 符合 {camera_id}_{timestamp}.jpg 格式 |
| UT-ST-003 | Base64编码 | 测试Base64编码保存 | 正确编码并返回 |
| UT-ST-004 | 目录创建 | 测试目录不存在时自动创建 | 自动创建目录 |

#### 3.1.7 数据库模块 (`database.py`)

| 测试编号 | 测试功能点 | 测试描述 | 预期结果 |
|---------|-----------|---------|---------|
| UT-DB-001 | 连接池初始化 | 测试连接池创建 | 正确创建指定数量连接 |
| UT-DB-002 | 获取摄像头列表 | 测试 get_all_cameras() | 返回所有摄像头配置 |
| UT-DB-003 | 保存检测记录 | 测试 save_detection_record() | 记录正确插入数据库 |
| UT-DB-004 | 轨迹合并 | 测试 merge_trajectory() | 时间窗口内轨迹正确合并 |
| UT-DB-005 | 连接池回收 | 测试连接超时回收 | 空闲连接正确回收 |
| UT-DB-006 | 并发安全 | 多线程并发数据库操作 | 无死锁，数据一致 |

#### 3.1.8 配置管理 (`config_loader.py`)

| 测试编号 | 测试功能点 | 测试描述 | 预期结果 |
|---------|-----------|---------|---------|
| UT-CF-001 | JSON配置加载 | 测试 config.json 加载 | 正确解析配置项 |
| UT-CF-002 | 默认值回退 | 配置项缺失时使用默认值 | 返回默认配置 |
| UT-CF-003 | 配置验证 | 测试无效配置处理 | 抛出适当异常 |
| UT-CF-004 | 动态重载 | 测试配置热重载(如支持) | 配置更新生效 |

---

### 3.2 集成测试功能点

#### 3.2.1 摄像头监测流程

| 测试编号 | 测试功能点 | 测试描述 | 预期结果 |
|---------|-----------|---------|---------|
| IT-CM-001 | 完整检测流程 | 视频流→变化检测→人员检测→存储 | 端到端流程正常 |
| IT-CM-002 | YOLO池集成 | CameraMonitor使用共享YOLO池 | 正确借用/归还实例 |
| IT-CM-003 | 多摄像头并发 | 多个CameraMonitor同时运行 | 互不干扰，资源正确共享 |
| IT-CM-004 | 异常恢复 | 模拟视频流中断 | 自动重连并继续监测 |

#### 3.2.2 数据库集成

| 测试编号 | 测试功能点 | 测试描述 | 预期结果 |
|---------|-----------|---------|---------|
| IT-DB-001 | 检测结果入库 | 检测到人员后数据入库 | 记录正确存储 |
| IT-DB-002 | 轨迹自动合并 | 连续检测同一人员 | 轨迹正确合并 |
| IT-DB-003 | 图片路径关联 | 图片保存与数据库记录关联 | 路径正确对应 |

#### 3.2.3 YOLO池配置集成

| 测试编号 | 测试功能点 | 测试描述 | 预期结果 |
|---------|-----------|---------|---------|
| IT-YP-001 | 全局池模式 | yolo_pool_id=NULL | 所有摄像头共享全局池 |
| IT-YP-002 | 独立实例模式 | yolo_pool_id=0 | 摄像头使用独立YOLO实例 |
| IT-YP-003 | 分组池模式 | yolo_pool_id=1,2,3 | 相同ID摄像头共享同一池 |

---

### 3.3 系统测试功能点

#### 3.3.1 性能测试

| 测试编号 | 测试功能点 | 测试描述 | 验收标准 |
|---------|-----------|---------|---------|
| ST-PF-001 | 单摄像头检测延迟 | 测量从帧输入到检测完成的时间 | < 500ms |
| ST-PF-002 | 多摄像头吞吐量 | 测试10路摄像头并发检测 | 总检测帧率 > 10fps |
| ST-PF-003 | 内存使用 | 监控长时间运行内存 | 无内存泄漏，稳定在阈值内 |
| ST-PF-004 | CPU使用率 | 监控CPU使用情况 | 平均使用率 < 80% |
| ST-PF-005 | YOLO池等待时间 | 测量实例借用等待时间 | 平均等待 < 100ms |

#### 3.3.2 稳定性测试

| 测试编号 | 测试功能点 | 测试描述 | 验收标准 |
|---------|-----------|---------|---------|
| ST-ST-001 | 长时间运行 | 系统连续运行24小时 | 无崩溃，功能正常 |
| ST-ST-002 | 网络抖动 | 模拟网络不稳定 | 自动恢复，不丢失检测 |
| ST-ST-003 | 高负载 | 模拟高并发检测请求 | 系统稳定，无死锁 |
| ST-ST-004 | 资源耗尽恢复 | 模拟内存/连接池耗尽 | 优雅降级，自动恢复 |

#### 3.3.3 功能验证测试

| 测试编号 | 测试功能点 | 测试描述 | 验收标准 |
|---------|-----------|---------|---------|
| ST-FN-001 | 人员检测准确率 | 测试集人员检测 | 准确率 > 90% |
| ST-FN-002 | 误报率 | 无人场景测试 | 误报率 < 5% |
| ST-FN-003 | 漏检率 | 有人场景测试 | 漏检率 < 10% |
| ST-FN-004 | 时间戳准确性 | 验证检测时间戳 | 误差 < 1秒 |

---

### 3.4 接口测试功能点

#### 3.4.1 Web API 接口 (`person_management_api.py`)

| 测试编号 | 测试功能点 | 接口 | 预期结果 |
|---------|-----------|------|---------|
| API-001 | 获取摄像头列表 | GET /api/cameras | 返回所有摄像头信息 |
| API-002 | 获取检测记录 | GET /api/detections | 返回检测记录列表 |
| API-003 | 手动确认记录 | POST /api/confirm | 确认状态更新成功 |
| API-004 | 参数验证 | 各接口参数校验 | 返回适当错误码 |
| API-005 | 分页查询 | 带分页参数查询 | 正确分页返回 |

---

### 3.5 异常测试功能点

| 测试编号 | 测试功能点 | 测试描述 | 预期结果 |
|---------|-----------|---------|---------|
| EX-001 | RTSP连接失败 | 无效RTSP地址 | 错误日志，不崩溃 |
| EX-002 | 数据库连接失败 | 数据库不可用 | 重试机制，错误日志 |
| EX-003 | YOLO模型加载失败 | 模型文件缺失 | 明确错误提示 |
| EX-004 | 磁盘空间不足 | 图片保存失败 | 错误处理，告警 |
| EX-005 | 许可证过期 | 许可证验证失败 | 明确提示，功能限制 |
| EX-006 | 配置文件错误 | JSON格式错误 | 启动失败，明确提示 |

---

## 四、测试策略建议

### 4.1 测试环境

| 环境类型 | 配置要求 |
|---------|---------|
| 开发测试 | 单机，1-2路模拟视频流 |
| 集成测试 | 单机，5路RTSP摄像头 |
| 性能测试 | 8核CPU，16GB内存，10路摄像头 |
| 稳定性测试 | 生产环境配置，24小时运行 |

### 4.2 测试数据

| 数据类型 | 描述 |
|---------|------|
| 测试视频 | 包含不同场景（有人/无人/多人/遮挡）的视频文件 |
| 测试图片 | 标注好的人员检测测试图片集 |
| 模拟RTSP | 使用FFmpeg搭建RTSP测试服务器 |
| 数据库 | 测试数据库，预置摄像头配置 |

### 4.3 测试优先级

| 优先级 | 测试类型 | 说明 |
|-------|---------|------|
| P0 | 核心功能 | 人员检测、变化检测、数据存储 |
| P1 | 集成测试 | 完整检测流程、多摄像头 |
| P2 | 异常处理 | 断线重连、错误恢复 |
| P3 | 性能测试 | 延迟、吞吐量、资源使用 |
| P4 | 边界测试 | 极端场景、边界值 |

### 4.4 自动化测试建议

```
tests/
├── unit/                    # 单元测试
│   ├── test_image_detection.py
│   ├── test_person_detector.py
│   ├── test_yolo_pool.py
│   ├── test_video_stream_reader.py
│   ├── test_input_shaper.py
│   ├── test_image_storage.py
│   ├── test_database.py
│   └── test_config_loader.py
├── integration/             # 集成测试
│   ├── test_camera_monitor_integration.py
│   ├── test_database_integration.py
│   └── test_yolo_pool_integration.py
├── system/                  # 系统测试
│   ├── test_performance.py
│   ├── test_stability.py
│   └── test_accuracy.py
├── api/                     # 接口测试
│   └── test_api_endpoints.py
└── fixtures/                # 测试数据
    ├── test_videos/
    ├── test_images/
    └── mock_configs/
```

---

## 五、已有测试文件

项目中已存在以下测试文件：

| 文件 | 描述 |
|------|------|
| `tests/test_input_shaper.py` | 输入整形层单元测试 |
| `tests/test_yolo_pool.py` | YOLO池单元测试 |
| `tests/test_system_integration.py` | 系统集成测试 |
| `tests/test_timestamp_consistency.py` | 时间戳一致性测试 |
| `run_tests.py` | 测试运行脚本 |
| `test_system.py` | 系统测试入口 |

---

## 六、测试执行命令

```bash
# 运行所有测试
python run_tests.py

# 运行特定测试模块
python -m pytest tests/test_input_shaper.py -v

# 运行系统集成测试
python test_system.py

# 运行带覆盖率的测试
python -m pytest tests/ --cov=. --cov-report=html
```

---

## 七、测试报告模板

测试执行后应产出以下报告：

1. **测试结果报告** - 通过/失败/跳过统计
2. **覆盖率报告** - 代码覆盖率分析
3. **性能报告** - 响应时间、吞吐量统计
4. **缺陷报告** - 发现的问题及严重程度

---

*文档版本: 1.0*
*生成日期: 2026-01-26*
