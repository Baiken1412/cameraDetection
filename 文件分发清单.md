# 软件许可证系统 - 简化版分发说明

## 核心原则

**没有私钥 = 无法生成有效许可证**

即使客户拿到完整的 `license_manager.py` 代码，没有 `private_key.pem` 也无法签发有效许可证！

---

## 文件说明

### 🔴 你保留的文件（不给客户）

| 文件名 | 说明 |
|--------|------|
| `generate_license.py` | 生成工具，方便你操作 |
| `private_key.pem` | **私钥 - 核心机密！** |

### 🟢 给客户的文件

| 文件名 | 说明 |
|--------|------|
| `license_manager.py` | 完整代码（没私钥也没用） |
| `public_key.pem` | 公钥 |
| `license.dat` | 许可证文件 |
| `main.py` 及其他业务代码 | 正常业务代码 |

---

## 为什么这样安全？

1. **许可证签名需要私钥**
   - 生成许可证时用 `private_key.pem` 签名
   - 客户没有私钥，无法伪造签名

2. **验证只需要公钥**
   - 验证许可证时用 `public_key.pem` 验证签名
   - 公钥可以公开，不影响安全

3. **RSA加密算法保证**
   - 有公钥无法推导出私钥
   - 无法伪造有效签名

---

## 操作流程

### 1. 首次设置
```bash
python generate_license.py --generate-keys
# 生成 private_key.pem（保密） 和 public_key.pem（给客户）
```

### 2. 为客户生成许可证
```bash
python generate_license.py --create --machine-code <客户机器码> --days 365
```

### 3. 发给客户
只需要注意：**不要把这两个文件给客户**
- ❌ `generate_license.py`
- ❌ `private_key.pem`

其他文件都可以给，包括 `license_manager.py`！

---

## 总结

**简单版规则：**
- 只保密 2 个文件：`generate_license.py` + `private_key.pem`
- 其他文件随便给客户
- 客户没私钥 = 无法生成许可证 = 安全
